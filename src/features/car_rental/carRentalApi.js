// src/features/car_rental/carRentalApi.js
import { apiSlice } from '@/features/api/apiSlice';

export const carRentalApi = apiSlice.injectEndpoints({
    endpoints: (builder) => ({
        // enpoinds here
    }),
});

export const {
} = carRentalApi;
