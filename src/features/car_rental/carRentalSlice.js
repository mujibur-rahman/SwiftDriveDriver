// src/features/car_rental/carRentalSlice.js
import { createSlice } from '@reduxjs/toolkit';

const initialState = {
};

const carRentalSlice = createSlice({
    name: 'car_rental',
    initialState,
    reducers: {
    },
});

export const { } = carRentalSlice.actions;
export default carRentalSlice.reducer;
